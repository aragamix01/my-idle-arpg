# Roadmap

One rule decides this order: **anything that changes where damage comes from goes
before anything that authors numbers.** That is why Phase 5's formula rework jumped
the queue and shipped as 5a-1 and 5a-2, and it is why weapons-and-skills now sits in
front of the uniques - a skill supplies the base that every modifier multiplies, so
uniques authored first would be authored twice.

Order:  1 -> 5a-1 -> 5a-2 -> 6a -> 6b -> 2 -> 5a-3 -> 3 -> 4 -> 5b -> 5c

Status: [done] shipped and verified  [next] in progress or up next  [todo] not started

THIS FILE IS THE SPEC. Read the relevant phase before starting work on it and update it
after finishing - what shipped, what diverged, and any spoken decision that overrode what
is written here. Phase 4 shipped eight divergences from this file because it was planned
from memory of an earlier read; every one of them had to be rebuilt.


## [done] Phase 1 - Crafting items & the dungeon
- Each stage drops 1 to 3 items at random
- Inventory raised to 200 (later than planned; started at 100) with sorting and filtering
- New currency dropping from the dungeon
- Bulk dissemble by explicit multi-select, not by filter sweep
currency
	basic
	- sacred idol -> next reroll rolls only prefixes
	- dark idol -> next reroll rolls only suffixes
	- magic ore -> upgrade common to magic
	- rare ore -> upgrade magic to rare
	- angel flame -> reroll the tier of each modifier, not which modifiers
	- angel droplet -> common becomes unique, or is destroyed (10% success)

	spirit
	> one spirit per item, ever, and never alongside another
	- bishop spirit -> rare only. trades a prefix row for a suffix row
	- devil spirit -> rare only. trades a suffix row for a prefix row
	- dune spirit -> rare only. removes 0-1 rows and adds 1, either side. the only route to an extra row

	fragment
	> not from the dungeon boss - from stage bosses, 0-3 per clear
	> also from dissembling (common -> magic ore, magic -> rare ore, rare -> angel flame, unique -> angel droplet)
	- 10 fragments combine into the currency they name

- Dungeon: a boss fight with a guaranteed item drop, scaling boss stats, 1-2 currency
- Spirits are far rarer than everything else
- Dungeon needs a key; keys drop from stage bosses at 20%
- Currency has its own stash tab, click to arm, gold to reroll
- Craft modal listing the armed currency; stays open so a player can reroll until satisfied
- Character button moved out and restyled
- Equipped items cannot be dissembled; rare/unique dissembles need confirmation
- Base implicit per item, never rerollable by gold or by any currency
- "artifact" renamed to "item" throughout


## [done] Phase 5a-1 - The three modifier layers
> Was Phase 5. Moved first: it replaces the damage formula, and Phase 2 and 4 both
> author numbers against that formula.

- Every modifier now declares a layer, and stats resolve as
      final = (base + sum flat) x (1 + sum increased) x product more
- flat and increased accumulate in a SUM, so their marginal value falls instead of
  rising. This is the fix for "it scales too fast": the old pool was all
  multipliers, so modifier N+1 was worth more than modifier N
- `more` is the only compounding layer. Reserved for uniques and the gold upgrade
  tracks, both bounded by something. A rollable `more` affix is rejected by
  validateRegistry
- Crit reworked: critChance = (base + flat) x (1 + increased), critMult additive
- Migration re-resolves every stored modifier value onto the new scale

Two things from the original plan turned out to be wrong and were dropped:
- Upgrade tracks CANNOT move to the `increased` layer. Enemy HP grows exponentially
  in stage; `increased` grows linearly in levels bought, and levels grow like
  log(gold). Linear against exponential walls permanently at any growth rate above
  1.0, and no retune fixes it. Tracks stay `more`
- Item-level-scaled flat tables are therefore unnecessary. A flat roll sits inside
  the multiplicative envelope, so every upgrade amplifies it and it never goes
  stale. No pegging, no ten-digit tooltips

Measured: drop ceiling 5.4x, craft 8.6x, offence/defence symmetry 0.008.
Stage 300 still reachable with no wall. Total elapsed shortened 4.7d -> 1.8d.


## [done] Phase 5a-2 - Modifier rows and the wider pool
- Rows per rarity: common 1 prefix 1 suffix, magic 2/2, rare 3/3
- The power ceiling deliberately did NOT move (5.4x drop, 8.6x craft). Per-affix
  values came down to pay for the rows. The gain is surface, not power: six
  modifiers to read instead of four, and a much wider gap between rarities
- Added a flat and an increased variant for each stat, since the two multiply each
  other - holding a lot of one makes the other the better pick
- Added defensive SUFFIXES. Widening the rows broke the offence/defence invariant
  structurally: offence could use 5 of its 6 rows, defence only 3, because every
  defensive affix was a prefix. Measured 0.47 asymmetry against a 0.15 bound
- Magic Ore and Dune Spirit descriptions corrected for the new row counts

Ladder after: stage 300 in 2.0d, clear times 18-29s, no wall.


## [done] Phase 6a - Weapons & skills, the foundation
> Was "Big change gameplay", at the bottom of the file. Moved to the front of the
> unbuilt work for the same reason Phase 5 was: it changes where damage COMES FROM,
> and Phase 2 authors eight uniques priced against that. Authoring them first means
> authoring them twice.

The good news, and the reason this is not another ground-up rework: **the three
layer system already accommodates skills.** Stats resolve as

      final = (base + sum flat) x (1 + sum increased) x product more

and `base` was always just a constant sitting in BASE_STATS. A skill supplying that
base instead of a hardcoded 60 is a small, well-isolated change. Nothing about the
layers, the affix pool, or the currency has to move.

- New item type: weapon, in its own equip slot
- A weapon carries a SKILL, and the skill supplies the bases the layers build on:
  base damage, base speed, base crit chance, base area
- Both playstyles CAN crit. Four of the nine suffixes are crit affixes, so taking
  crit away from casters would kill half their suffix pool. Differentiate by base
  value instead: physical skills get high base crit, spells get near-zero. That is
  the "each skill has base crit chance" note, and it costs nothing
- No resources in this step - resources are 6b

THE AXIS OF DIFFERENCE IS AREA VERSUS SINGLE-TARGET, and the combat layer already
has it. Every stage is trash-then-boss: the trash phase multiplies damage by
min(area, enemyCount), and the boss phase ignores area entirely.

	physical   high per-hit, area 1-2, high base crit   -> melts bosses, slogs trash
	magical    lower per-hit, area 4-8, low base crit   -> shreds trash, grinds bosses

STAGE_TIME_LIMIT_SECONDS is 75 for the TOTAL, so each type has a real failure mode -
pure physical times out on trash, pure magic times out on the boss. That is what
makes the choice a choice rather than a dominance ordering, and it needs no new
systems.

SKILL LEVEL comes from the weapon's item level: floor(itemLevel / 10), plus
modifiers granting "+N to Physical Skill Levels" or "+N to Magical Skill Levels",
weapon-type-locked. Each level raises the skill's BASE by about 4%, so a stage-300
weapon is roughly 2.2x the base of a shallow one.

- This fixes a real gap: item level currently does nothing above stage 80, because
  that is the last tier gate. Skill level makes depth matter forever and makes the
  weapon the chase item
- Side effect worth knowing: skill level raises `base`, so it COMPETES with flat
  damage while increased and more multiply the result. A third position in the layer
  tension, for free
- Risk: this is a second exponential engine beside the gold tracks, which are tuned
  so the feedback exponent stays below 1. At 4% per level it is a modest term rather
  than a second engine, which is why that number and not a bigger one. Making
  weapons BECOME the power curve is the bolder option and would mean retuning the
  gold track exponents down to compensate


## [done] Phase 6b - The magical playstyle
> Split from 6a because mana is the only genuinely new machinery here, and it is the
> one piece that touches the combat layer rather than the stat layer.

Physical pays STAMINA, magic pays MANA. Both types pay something, so neither is
free - which was the flaw in the first cut of this plan: mana with no matching cost
on physical made magic strictly worse and nobody would ever pick a wand.

ONE resource system, named by the weapon's kind. Same stats, same math, two labels
in the UI. That halves the stat surface and there is no gameplay in having two
implementations of one idea.

	sustained rate = min(speed, regen / costPerUse)
	DPS            = perHit x sustainedRate x critFactor x aoeTargets

Both closed-form, which is non-negotiable: resolveStage has no per-tick loop, and
that is what lets the server resolve offline progress and the harness sweep 300
stages in milliseconds. A tick loop would be correct in the small and would throw
away the two-layer architecture, offline resolution and the balance harness.

THE TWO RESOURCES DIFFER IN WHEN THEY BITE, not in their math:

	              pool    regen   cost    binds
	stamina       small   fast    cheap   LATE, once attack speed is stacked
	mana          large   slow    high    IMMEDIATELY, from the first cast

So physical ignores stamina for a long time and then hits a wall - it has bought
attack speed and is not actually swinging faster, and stamina regen becomes the
affix that unlocks the attack speed it already owns. Magic gears mana from the first
hour or it does not function. Different gearing arcs out of three numbers.

Bonus: stamina is the soft cap attack speed does not currently have. Attack speed is
an uncapped `more` track plus affixes with nothing bounding it.

New affixes, weapon-type-locked:
- resource regen, resource cost reduction (stamina on physical, mana on magical)
- + to Physical Skill Levels, + to Magical Skill Levels

NOT adding a separate cast-speed stat. One speed stat scaled by the skill's own base
speed does the same job; a twin would mean a duplicate of every speed affix for no
difference in outcome.

Weapon-restricted affixes need a tag on AffixDefinition naming which base types may
roll them, plus a filter in rollAffixes. Small schema addition, no migration.

PHYSICAL-VS-MAGICAL PARITY becomes a third invariant beside offence/defence symmetry
and the feedback exponent. Whichever playstyle runs ahead makes the other dead
content, and it will not hold by accident - the offence/defence one has broken
STRUCTURALLY three times now. The test compares TOTAL clear time, not per-phase:
magic is meant to be faster on trash and slower on the boss, and a per-phase check
would fail on the difference that is the whole point.

Two things to measure, not assume:
- Magic may out-earn physical. farmRate is killsPerSecond x goldPerKill and area
  raises kills/sec directly, so unless the lower per-hit offsets it exactly, casters
  become the economic pick as well as the wave pick
- Area is capped at min(area, enemyCount) and enemyCount caps at 220. Past that,
  magic's area stops paying and its scaling has to come from per-hit and speed - so
  magic's late game plays differently from its early game


## [next] Phase 2 - Unique items
> SHIPPED so far: the mechanism (tiers setting drop weight, values rolling inside an
> authored range, Angel Flame as the only currency that touches a unique) and four of
> the roster - Berserker's Anvil, Duelist's Visor, Warden's Coffer, Bulwark of the
> Deep.
>
> STILL OPEN, and blocked on two different things:
> - The two SLOT uniques and the "other equipped items x200-500%" one need equip
>   slots to become a derived number instead of a constant. That is a save-shape
>   change: loadout length, the equipItem bound, an inert-slot state in the UI, and a
>   rule for which items may change the count at all (only ones in the first four
>   positions, or the fixed point runs away)
> - ART. The tile sheet has ~94 unbound tiles and almost all are floor, wall, door or
>   fence; the free ones that read as an OBJECT at 16px are down to furniture and
>   townsfolk. The four shipped uniques took the last four usable ones. A fifth needs
>   a second CC0 pack as a new GridSource - the indirection exists for exactly this
>   and no game code moves
>
> Also dropped deliberately: the "area is 0, attack speed x150-300%" entry, which is
> the same trade as Berserker's Anvil with a different stat, and the dungeon-currency
> twin of Warden's Coffer, which is the same trade as the key one.
> Moved back behind weapons. Two of the seven uniques below grant or remove EQUIP
> SLOTS, and 6a adds a slot - resolving that interaction once is cheaper than
> resolving it twice. Authoring uniques that can be physical or magical is also only
> possible after skills exist.
>
> If something shipping sooner matters more than authoring once, this can be pulled
> back in front of 6a. The cost is re-tuning eight roll tables and redesigning the
> two slot uniques.

- Unique TIER first, setting drop weight - how hard each one is to obtain. Build the
  mechanism now, fill it in as more uniques are added
- Uniques roll their values within an authored range; Angel Flame is the only
  currency that touches them
- One rule: a unique may carry a downside, and then a fantasy payoff on top
- The seven from the original list:
	- area + (0.25 - 2) / grants 1-3 extra equip slots
	- attack + (2% - 10%) / no crit chance at all, but attack x (150% - 300%)
	- attack + (2% - 10%) / area is 0, but attack speed x (150% - 300%)
	- crit rate - (30% - 50%) / crit damage x (3 - 5)
	- gold drop - (30% - 60%) / stage boss key drops doubled
	- gold drop - (30% - 60%) / dungeon boss currency doubled
	- equip slots - (1-2) / other equipped items' effects x (200% - 500%)
- Add a defensive unique too. Every one above is offence or economy, and a unique
  tier with no defensive chase item fails the symmetry invariant structurally - the
  same fault that hit Phase 1's affix pool and again in 5a-2

Uniques are already the only content using the `more` layer, which is what makes
one feel like a different class of item rather than a rare with a name.


## [done] Phase 5a-3 - Elements, resistances, penetration

Both open questions were settled by measuring rather than by argument:

- **A hit is SHARES of one number by element**, not four damage pools with four sets of
  layers. The skill's own element sits at 1 and each `gain X% as extra` affix adds a
  share, so the DPS a target takes is `damage x speed x critFactor x Σ share x
  mitigation(element)`. One map plus one stat instead of four of everything
- **ONE penetration stat**, not four. Four means four affixes competing for the same
  rows, each dead unless it matches the element you happen to deal - book-keeping dressed
  as a build decision
- Enemy resistance rises with stage toward `RESISTANCE_CAP`, uniform across elements on
  the ladder. **0.25, and measured**: the first cut at 0.40 doubled the ladder, 7.8d to
  16.0d. Not a wall - clear times were fine - but a pacing change wearing a scaling
  axis's clothes
- Dungeons and the Abyss carry an AFFINITY instead: one element resisted, one vulnerable,
  derived from the account seed so it is knowable before the key or tablet is spent.
  0.35 for a dungeon, 0.55 for the Abyss. 0.70 was rejected - it puts the resisted case
  at x0.12, which against a tier-scaled boss is a timeout, which is "impossible" wearing
  a different word

So elements are a scaling axis you can ignore on the ladder and the whole fight in the
content that carries an affinity, which was the brief.

**The gap this left, and it is now the most visible thing in the game:** four weapons
cover three elements. Lightning and darkness have no weapon at all, so an affinity
resisting either is a free pass, and one resisting your only element has no answer -
conversion is not one at 1.2-3.3% against a x0.27. See Phase 4's closing note.


## [mostly] Phase 3 - More loot

Shipped:

- Monster waves drop items, 5% per kill capped at 6 a clear, and they fall on screen out
  of the enemy that dropped them. Rolled client-side from the same pure function the
  server uses, so what you watch fall is what lands in the inventory
- **Wave loot draws from its own rarity table**, and unique is ZERO there. That is
  structural rather than flavour: wave loot multiplies drop volume several times over, so
  sharing the normal table would multiply the unique rate with it and quietly undo the
  tier weights. Volume changes what you have to dissemble, never what you are hunting
- Tablets ride the same faucet at 0.1% - see Phase 4
- Measured across four seeds before and after: throughput quadrupled, **pacing did not
  move**. Baseline 8.2/7.1/12.8/4.2d against 10.1/11.4/9.6/4.8d - mixed signs, spread
  dwarfing the difference. A single seed cannot tell a pacing change from drop luck
- Offline play: a welcome-back receipt, auto-claimed, so gold is never owed and
  uncollected

Still open:

- **Overflow inventory.** A full inventory swallows the rest of the wave and says how
  many were lost; there is no temporary shelf to clear. INVENTORY_CAP is 200 now and
  wave loot puts it under real pressure, so this is a live gap rather than a nicety
- **Currency menu descriptions.** Still the known gap it was
- **Wave loot on a failed attempt.** Gold pays partial credit through `fractionOfTrash`;
  items pay nothing. Same kills, two rules - unresolved, and worth resolving


## [done] Phase 4 - Tablets & the Abyssal dungeon

Everything below shipped. Two things were decided in conversation and OVERRIDE what was
originally written here:

- **Tier 1-15 as well as rarity**, PoE2-waystone style. Rarity buys explicit rows exactly
  as it does on an item; the tier is the difficulty and the wave count. The original
  "tiers like items (common, magic, rare)" is the rarity half only
- **The Abyss unlocks at floor 80** - the last affix gate, so it is the floor where the
  ladder stops paying in gear and starts paying in tablets

What shipped:

- Monster waves drop tablets at 0.1% per kill, past floor 80. The ladder tops out at
  **T3, one tier per 60 floors** - the Abyss owns the tier ladder, and a ladder that
  reached the top made the mode's own tier-up reward pointless (measured: it collapsed
  10th-percentile clear time to 4.4-6.9s against a 12s floor)
- A tablet **IS an `ItemInstance`**: rarity is the row count, `itemLevel` is the tier,
  `baseAffix` is the implicit, `affixes` are the explicits. The affix pool is partitioned
  by `rollsOn: 'tablet'` rather than the type being forked
- Explicits are prefix/suffix affixes that ONLY buff monsters - four axes, hp / damage /
  count / waves. They produce no `Effect` at all, so there is no code path from a tablet
  modifier to `deriveStats`
- The implicit pays, and its magnitude is the rolled value times `(1 + Σ explicits)`.
  One knob: an explicit is pure downside and the implicit is what turns it into a payout
- Waves then the boss, wave count from the tier. **Health carries across the whole run**
  and nothing refills it, so `waves` is a danger axis rather than a time cost
- Delves get their own time limit, `STAGE_TIME_LIMIT_SECONDS x (1 + waves x 0.5)`
- Tablets take crafting currency, in their own tab, capped at 200. Spirits and the unique
  gamble are refused with reasons; gold reroll is off because `rerollCost` scales on item
  level and a tablet's is its tier
- A boss entrance beat between the wave and the boss, costing real time and no simulated
  time

Still open, and visible now that the mode exists:

- **Lightning and darkness have no weapon.** Four weapons, three elements, so an Abyssal
  run resisting either is a free pass. The affinity is the biggest number in the fight
  and there is no answer to two of the five cases
- The extra-element affixes are 1.2-3.3%, nothing against a resisted x0.27, so conversion
  is not an answer either. Growing them is its own decision


## [next] Phase 5b - Accessories
> Split out of the original Phase 5. Its drop source is the Abyssal dungeon, so it
> belongs with Phase 4 rather than with the formula rework.

- New item type: ring and amulet
- Accessories have tiers and take most crafting currency like any item
- Accessory modifiers are always percentage
- 2 ring slots and 1 amulet slot on the character
- Drop only from the Abyssal dungeon, 1-2 guaranteed, related to the tablet implicit


## [todo] Phase 5c - The upgrade panel
> Also split out of Phase 5. Independent of everything else.

- Rework the stat upgrade buttons
- Each track now declares which layer it feeds, so the panel can say "7% more
  damage" for a compounding track and "+0.25 targets" for a flat one - it already
  does, but the panel is otherwise unchanged from before layers existed


## Later, not scheduled
- Character skill tree and passives. Named in the weapons notes as coming after
  skills-from-weapons, and it is a whole progression system of its own


## Known issues, unrelated to any phase
- TOTAL LADDER TIME IS DOMINATED BY UNIQUE DROP LUCK, and the golden snapshot is one
  sample of it. Swept across six account seeds after Phase 2: 5.3d, 5.5d, 6.7d,
  14.4d, 17.9d, 18.0d. The fast runs are the ones that found Deep Delver's Idol or
  The Whetstone; the slow ones never did. Uniques are 2% of drops split twelve ways,
  and one of them carries `goldFind more 2.0` - so the economy hinges on a lottery.
  Worth deciding whether that spread is the intended shape of the game before reading
  any single "reached stage 300 in Xd" as a pacing statement
- /api/health passes Supabase error strings straight to a public endpoint. Delete it
- Gold no longer overflows - it is a decimal string in the save and a Big everywhere it
  is arithmetic, since 5a-1's Phase 7. `damage` and `maxHp` are Bigs for the same reason.
  Attack speed and resource regen are the two stats that could still overflow, at roughly
  18,000 levels, which the cost curve reaches only far beyond stage 1e5
- Prestige vs big numbers is still undecided. `lifetimeGold` exists and is credited, so
  the input is there; what it BUYS is the undecided half
- **The element roster is short two weapons.** Lightning and darkness have no weapon, so
  two of the five Abyssal affinities are a free pass and one of the other three has no
  answer. The cheapest fix is two weapon bases; the deeper one is making conversion
  viable, which means growing the extra-element affixes and re-deriving the power budget
- **The Raven pack's licence is unverified.** No licence file shipped with it, only a
  thank-you note, and its art is on every item. CREDITS.md records the uncertainty rather
  than guessing at a licence

## Verification, for any phase

`pnpm exec tsc --noEmit`, `pnpm exec eslint . --max-warnings 0`, `pnpm exec vitest run`,
`pnpm exec playwright test`, then `pnpm balance` and READ the diff.

Two habits this project has paid for the hard way:

- **A single seed cannot tell a pacing change from drop luck.** Sweep four with
  `runLadder(seed)` before claiming anything moved. Total ladder time spans 4.8-11.4d
  across seeds on identical code
- **The browser pane does not composite**, so requestAnimationFrame never fires there and
  the replay never advances. Anything in the cosmetic layer has to be driven from node -
  `tests/cosmetic.test.ts` - or it is not being tested at all. Playwright does composite
